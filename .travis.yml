sudo: false
language: go
go:
  - "1.x"
go_import_path: hello-travis
branches:
  only:
  - dev
  - master
before_install:
  - curl https://raw.githubusercontent.com/golang/dep/master/install.sh | sh
install:
  - dep ensure
script:
  - go test -v -race ./...
  - go vet ./...

